# React Flow Sample

Minimal React Flow playground for trying different hardcoded scenarios fast.

## Run

```bash
npm install
npm run dev
```

The Vite dev server is pinned to `http://localhost:4400`.

## What is included

- three hardcoded scenarios
- a simple scenario switcher
- draggable nodes and editable connections
- no persistence, auth, backend, or import/export
